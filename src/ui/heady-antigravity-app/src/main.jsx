import React from 'react'
import ReactDOM from 'react-dom/client'
import HeadyAntigravityUI from './HeadyAntigravityUI'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <HeadyAntigravityUI />
    </React.StrictMode>,
)
