name: Learning Pipeline
on:
  schedule:
    - cron: '0 12 * * *'
jobs:
  learning:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Run learning lane, summary generation, and pattern promotion"
