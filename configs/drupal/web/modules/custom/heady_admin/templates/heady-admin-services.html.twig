{# Services Management Template #}
<div class="heady-admin-wrapper">
  <nav class="heady-admin-nav">
    <div class="heady-admin-logo"><span class="heady-admin-logo-icon">🌟</span><span>Heady Admin</span></div>
    <div class="heady-admin-tabs">
      <a href="{{ path('heady_admin.dashboard') }}" class="heady-tab">📊 Dashboard</a>
      <a href="{{ path('heady_admin.services') }}" class="heady-tab active">🔌 Services</a>
      <a href="{{ path('heady_admin.hcfp') }}" class="heady-tab">🛡️ HCFP</a>
      <a href="{{ path('heady_admin.headylens') }}" class="heady-tab">🔭 HeadyLens</a>
      <a href="{{ path('heady_admin.customize') }}" class="heady-tab">🎨 Customize</a>
      <a href="{{ path('heady_admin.access_control') }}" class="heady-tab">👥 Access</a>
      <a href="{{ path('heady_admin.settings') }}" class="heady-tab">⚙️ Settings</a>
    </div>
  </nav>

  <div class="heady-section">
    <h2 class="heady-section-title">Ecosystem Services ({{ ecosystem|length }})</h2>
    <div class="heady-ecosystem-grid">
      {% for svc in ecosystem %}
        <div class="heady-eco-card" style="border-color: {{ svc.color }}40;">
          <div class="heady-eco-icon" style="background: {{ svc.color }}22; color: {{ svc.color }};">{{ svc.icon }}</div>
          <h3>{{ svc.name }}</h3>
          <p class="heady-eco-geometry">{{ svc.geometry }}</p>
          <a href="{{ svc.url }}" target="_blank" class="heady-eco-link" style="color: {{ svc.color }};">Visit →</a>
          <span class="heady-eco-status online">● Online</span>
          <div style="margin-top:0.75rem;display:flex;gap:0.3rem;">
            <button class="heady-btn heady-btn-sm heady-btn-secondary" title="Restart">🔄 Restart</button>
            <button class="heady-btn heady-btn-sm heady-btn-secondary" title="Config">⚙️ Config</button>
            <button class="heady-btn heady-btn-sm heady-btn-danger" title="Stop">⏹️ Stop</button>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>

  <div class="heady-section">
    <h2 class="heady-section-title">Internal Services</h2>
    <div class="heady-table-container">
      <table>
        <thead><tr><th>Service</th><th>Type</th><th>Status</th><th>Domain</th><th>Port</th><th>Actions</th></tr></thead>
        <tbody>
          {% for service in services %}
            <tr>
              <td><strong>{{ service.name }}</strong></td>
              <td><span class="heady-type-badge">{{ service.type }}</span></td>
              <td><span class="heady-status-badge status-{{ service.status }}">● {{ service.status|capitalize }}</span></td>
              <td>{{ service.domain }}</td>
              <td>{{ service.port }}</td>
              <td class="heady-actions">
                <button class="heady-btn heady-btn-sm heady-btn-secondary">🔄</button>
                <button class="heady-btn heady-btn-sm heady-btn-secondary">⚙️</button>
                <button class="heady-btn heady-btn-sm heady-btn-danger">⏹️</button>
              </td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
    <button class="heady-btn heady-btn-primary" style="margin-top:0.75rem;">+ Add Service</button>
  </div>
</div>
<script src="https://headybuddy.org/headybuddy-widget.js"></script>
